<!DOCTYPE html>
<html>
<head>
  <title>Beluga Home</title>
</head>
<body>
  <h1>Home!</h1>

    <form method="POST" action="/sign-in">
      <label for="username">Username</label>
      <input type="text" name="username">
      <label for="password">Password</label>
      <input type="text" name="password">
      <button type="submit">Go!</button>
    </form>

  <h2>This page should have</h2>
  <ul>
    <li>Sign-in for if not signed in</li>
    <li>Link to own profile if signed in</li>
    <li>"newsfeed"</li>
    <li>list of users that link to their profiles</li>
    <li>link to show comments</li>
  </ul>
  <hr>
  <ul>
      <% @users.each do |user| %>
        <li>
          Username: <%= user.username%> <a href="/user/delete/<%=user.id%>">Delete</a>
          <br>
          Password: <%= user.password%>
        </li>
      <% end %>
  </ul>
  <hr>
    <ul>
      <% @posts.each do |post| %>
        <li>
          <a href=<%=post.url%>><h2><%= post.title%></h2></a>
          <p><%=post.body%></p>
          <ul>
            <% post.comments.each do |comment| %>
              <li>
                <p><strong><%=comment.user.username%></strong></p>
                <p><%=comment.body%></p>
              </li>
            <% end %>
          </ul>
        </li>
      <% end %>
  </ul>
</body>
</html>