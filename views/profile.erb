<%= flash[:notice] if flash[:notice] %>

<img src=<%=@user.pic%> class="prof-pic">

	<div class="page-header">
		<h1><%=@user.username%>
<% if session[:user_id] && session[:user_id]==@user.id %>
			<small>
				<form method="post" action="/user/<%=@user.id%>/delete" id="delete-user" style="display: inline-block">
					<a href="javascript:{}" onclick="document.getElementById('delete-user').submit();">(delete)</a>
				</form>
				<a href="/edit-profile/<%=@user.id%>">(edit)</a>
			</small>
<% end %>
		</h1>
	</div>

<p><strong>Email:</strong> <%=@user.email%></p>
<p><strong>Description:</strong> <%=@user.description%></p>

<% if session[:user_id] && session[:user_id]==@user.id%> 
	<br>
	<!-- <form method="post"><button formaction="/user/<%=@user.id%>/delete" type="submit">Delete User</button></form>
	<h2>New Post:</h2>
	<form method="POST" action="/post/new">
	  <label for="url">URL</label>
	  <input type="text" name="url">
	  	<br><br>
	  <label for="title">Title</label>
	  <input type="text" name="title">
	  	<br><br>
	  <label for="body">Body</label>
	  <textarea name="body" rows="4" cols="50"></textarea>
	  	<br><br>
	  <label for="tag">Tag</label>
	  <input type="number" name="tag" min="1" max=<%=Tag.count%>>
	  	<br><br>
	  <button type="submit">Post!</button>
	</form> -->
	<%= erb :new_post %>
<% end %>


<h2>Posts</h2>


<%= erb :post_card%>

<hr>

<h2>This page should have:</h2>
	<ul>
		<li>User's Profile Information</li>
		<li>Link to edit/delete profile if signed in</li>
		<li>List of User's Posts</li>
		<li>Option to edit posts if signed in</li>
		<li>Form to create post if signed in</li>
		<li>Display tag for posts</li>
	</ul>